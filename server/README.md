# Creddie Webserver

The `backend` directory contains all the files that are used to build the Creddie backend service.
The `frontend` directory contains the code to serve webpages to interact with your creddie database.